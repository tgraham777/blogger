<h1>All Tags</h1>

<ul id="tags">
  <% @tags.each do |tag| %>
    <li>
      <%= tag.name %><br>
      <%= link_to "Delete", tag, method: :delete, data: {confirm: "Really delete the article?"} %><br>
    </li>
  <% end %>
</ul>
