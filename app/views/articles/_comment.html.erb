<div>
  <h4>Comment by <%= comment.author_name %></h4>
  <p>Posted <%= distance_of_time_in_words(comment.article.created_at, comment.created_at) %> after article.</p>
  <p class="comment"><%= comment.body %></p>
</div>
